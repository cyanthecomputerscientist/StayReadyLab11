public class reversethenadd 
{
    //check if its a palindrom 
    // add the reverse to the number 
    public Boolean isPalindrome(String input)
    {
        return true;
    }
    public Integer reverseNumber(String input)
    {
        return 0;
    }
    public String reverseThenadd(String input)
    {
        String output = "";
        Integer number = Integer.parseInt(input);
        while(isPalindrome(Integer.toString(number))== false)
        {

            Integer reverse = reverseNumber(Integer.toString(number));

        }
        return output;
    }
}